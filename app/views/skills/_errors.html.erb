<% if skill.errors.any? %>
  <div class="error_message alert alert-warning">
    <h2><%= skill.errors.count %> 件のエラーが発生:</h2>
    <ul>
      <% skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>