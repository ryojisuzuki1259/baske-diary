<% if diary.errors.any? %>
  <div class="error_message alert alert-warning">
    <p><%= diary.errors.count %> 件のエラーが発生:</p>
    <ul>
      <% diary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>