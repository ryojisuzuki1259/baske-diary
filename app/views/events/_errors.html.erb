<% if event.errors.any? %>
  <div class="error_message alert alert-warning">
    <p><%= event.errors.count %> 件のエラーが発生:</p>
    <ul>
      <% event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>